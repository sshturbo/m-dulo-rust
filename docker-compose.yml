version: "3.8"

services:
  postgres:
    image: postgres:14.13
    container_name: postgres_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: cb8c87193f183f91a16e6eb697ca84ec
    ports:
      - "5432:5432" 
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - rede_geral
    restart: always
    deploy:
      resources:
        limits:
          memory: 1024M
          cpus: "0.5"

volumes:
  postgres_data:
    external: false
    name: postgres_data

networks:
  rede_geral:
    external: false
    name: rede_geral
